#!/bin/bash

echo 'Arrêt de la VM avant ajout CDROM\n'
govc vm.power -off -u 'https://{{esxi_username}}:{{esxi_password}}@{{esxi_hostname}}' -k "{{ inventory_hostname }}"
echo 'Ajout CD ROM\n'
govc device.cdrom.add -u 'https://{{esxi_username}}:{{esxi_password}}@{{esxi_hostname}}' -k -vm "{{ inventory_hostname }}"
sleep 60
govc device.connect -u 'https://{{esxi_username}}:{{esxi_password}}@{{esxi_hostname}}' -k -vm "{{ inventory_hostname }}"
echo 'Ajout de ISO windows 10\n'
govc device.cdrom.insert -vm "{{ inventory_hostname }}" -u 'https://{{esxi_username}}:{{esxi_password}}@{{esxi_hostname}}' -k  '[datastore1] ISOs/Win10_20H2_French_x64.iso'
echo 'Boot de la VM\n'
govc vm.power -on -u 'https://{{esxi_username}}:{{esxi_password}}@{{esxi_hostname}}' -k "{{ inventory_hostname }}"
sleep 80
echo "Fin du Script"
